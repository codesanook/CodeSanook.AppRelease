@model CodeSanook.AppRelease.Models.PostedFile
@{
    Layout.Title = T("Add app release");
}

@using (Html.BeginForm("Add", "Admin", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryTokenOrchard()
    @Html.ValidationSummary()

    <fieldset>
        @Html.LabelFor(m => m.File)
        @Html.TextBoxFor(m => m.File, new { type = "file" })
        @Html.ValidationMessageFor(m => m.File, "*")
    </fieldset>

    <fieldset>
        @Html.LabelFor(m => m.VersionNumber)
        @Html.TextBoxFor(m => m.VersionNumber, new { @class = "text medium" })
        @Html.ValidationMessageFor(m => m.VersionNumber, "*")
    </fieldset>

    <fieldset>
        <input class="primaryAction" type="submit" value="@T("Add")" />
    </fieldset>
}